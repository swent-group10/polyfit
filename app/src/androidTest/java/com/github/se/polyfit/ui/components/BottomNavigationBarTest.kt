package com.github.se.polyfit.ui.components
/*
import androidx.compose.ui.test.junit4.createComposeRule
import androidx.navigation.NavController
import androidx.test.ext.junit.runners.AndroidJUnit4
import io.mockk.impl.annotations.RelaxedMockK
import io.mockk.mockk
import junit.framework.TestCase
import org.junit.Rule
import org.junit.Test
import org.junit.runner.RunWith

@RunWith(AndroidJUnit4::class)
class BottomNavigationBarTest : TestCase() {

  @get:Rule val composeTestRule = createComposeRule()

  @RelaxedMockK var mockNav: NavController = mockk(relaxed = true)

  @Test
  fun homeButtonNavigatesToHome() {
    composeTestRule.setContent {
      BottomNavigationBar(mockNav, navHome = {}, navSearch = {}, navSettings = {})
    }
    // Assuming BottomNavigationBarScreen is defined with home, search, and settings properties
    BottomNavigationBarScreen(composeTestRule).apply {
      home.assertIsDisplayed()
      //            search.assertHasClickAction()
      //            settings.assertHasClickAction()
    }

    //        composeTestRule.onNodeWithTag("MainBottomBar").onChildAt(0).performClick()

  }
}

 */
